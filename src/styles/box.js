import * as constants from './constants'

const { colors } = constants

export default {
  clause: {
    background: colors.light,
    // padding: '10px',
    // margin: '0px 10px 10px 10px',
    // boxShadow: '2px 1px 3px -1px rgba(50, 50, 50, 0.47)'
  },
  hr: {
    margin: '0 0 0 8'
  }
}
