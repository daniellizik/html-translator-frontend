// use as exceptions for mutations
// we need to be able to mutate text of child node
// flat list doesnt play nicely, so we need to do some 
// mapping via view ids

export const APPLY_TO_CHILDREN = (node, mutation, list, ruleResult) => {

}

export const APPLY_TO_NODE = (node, mutation, list, ruleResult) => {
  // console.log(node, ruleResult)
}